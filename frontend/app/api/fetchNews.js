import ApiKeys from "./../constants/ApiKeys";
const BASE_URL = "http://newsapi.org/v2/";
const ENDPOINT = "top-headlines?";
const QUERY = "covid-19";

export default (async function fetchNewsAsync() {
  // return covid stats from endpoint
  return fetch(``, {
    method: "GET",
  });
});
